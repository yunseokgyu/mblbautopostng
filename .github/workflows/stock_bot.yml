name: Stock Bot Hourly Run

on:
  # schedule:
  #   - cron: '0 */6 * * *' # 6시간마다 실행 - 정지 요청으로 주석 처리0분에 실행 (24회/일) - 정지 요청으로 주석 처리
  workflow_dispatch:      # 수동 실행 가능

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Stock Bot
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        WP_URL: ${{ secrets.WP_URL }}
        WP_USER: ${{ secrets.WP_USER }}
        WP_PASSWORD: ${{ secrets.WP_PASSWORD }}
      run: |
        python stock_bot.py # --loop 옵션 없이 1회 실행

    - name: Commit and Push History
      # 봇이 history 파일을 업데이트했으면 그걸 저장소에 반영
      run: |
        git config --global user.name "StockBot Action"
        git config --global user.email "action@github.com"
        
        # 변경된 파일이 있는지 확인
        git add stock_data/published_history.json bot_status_stock.json
        
        # 변경사항이 있으면 커밋 & 푸시
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Update bot history [skip ci]" && git push)
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
